import React from 'react';
import ReactDOM from 'react-dom/client';
import WhisperMVP from './WhisperMVP';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WhisperMVP />
  </React.StrictMode>
);
